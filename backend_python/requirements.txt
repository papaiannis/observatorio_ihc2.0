# ==============================================================================
# Backend FastAPI — Identificación de Biodiversidad
# Instalar con: pip install -r requirements.txt
# ==============================================================================

# Framework web ASGI
fastapi==0.115.6

# Servidor ASGI (con soporte para websockets y reload en dev)
uvicorn[standard]==0.32.1

# Cliente HTTP asíncrono para llamar a Hugging Face Inference API
httpx==0.27.2

# Parsing de multipart/form-data (necesario para UploadFile en FastAPI)
python-multipart==0.0.12

# Gestión de variables de entorno con tipado estricto (Pydantic v2)
pydantic-settings==2.6.1

# Validación real del tipo MIME de archivos (no confía solo en la extensión)
# Requiere libmagic instalado en el sistema:
#   Windows: pip install python-magic-bin (incluye la DLL)
#   Linux/macOS: sudo apt install libmagic1 / brew install libmagic
python-magic==0.4.27
python-magic-bin==0.4.14; sys_platform == "win32"

# Cliente oficial de Hugging Face — necesario para InferenceClient con proveedor fal-ai
# Usado por whisper_client.py para transcripción de audio con openai/whisper-large-v3
huggingface_hub>=0.23.0

# Parseo avanzado de cabeceras de audio (necesario ya que Android soporta M4A/3GP nativo)
mutagen==1.47.0
requests==2.31.0

# Cliente para YouTube Data API v3
google-api-python-client==2.126.0

# Caché en memoria para optimizar peticiones
cachetools==5.3.3
